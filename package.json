{
  "name": "projeto-estoque",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev --prefix gerencie-api",
    "dev:web": "npm run dev --prefix gerencie-web-admin-front",
    "setup": "npm install --prefix gerencie-api && npm install --prefix gerencie-web-admin-front && npm run db:setup --prefix gerencie-api",
    "db:setup": "npm run db:setup --prefix gerencie-api"
  }
}
